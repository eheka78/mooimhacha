# 무임하차 클라이언트 환경 변수 (Electron + Vite)
# 이 파일을 .env로 복사한 뒤 값을 채워 사용한다.
#   cp .env.sample .env
# Vite는 VITE_ 접두사 환경 변수만 렌더러 번들에 노출한다.

# 서버 엔드포인트 (개발 시 localhost, 운영 시 EC2 퍼블릭 IP)
VITE_API_BASE_URL=http://localhost:3000
VITE_WS_URL=ws://localhost:3000
